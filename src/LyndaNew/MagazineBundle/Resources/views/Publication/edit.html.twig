{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Publication edit</h1>

    {{ form(edit_form) }}

        <a class="btn btn-default btn-xs" href="{{ path('publication') }}">
            Back to the list
        </a>
    {{ form(delete_form) }}
{% endblock %}
